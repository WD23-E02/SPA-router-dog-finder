import { useParams } from "react-router-dom"
import Dog from "./Dog"
import Nav from "./Nav"

export default function Dogs({ dogs }) {
    

    return (
      <div className="Dogs">
       {/* renderNav component and pass dog name  as prop*/}

        {/* If we have a name / if a dog is selected, then show info for that dog */}
       {/* render Dog component and pass current dog name as prop */}
      </div>
    )
  }
  